# gb-531 化工 HAZOP 保护层覆盖分析

请生成一个名为 `hazop-safeguard-coverage` 的 Go 全栈 Web 项目，面向化工企业的工艺安全团队，用于维护工艺节点、分析 HAZOP 偏差、登记独立保护层并计算保护覆盖缺口。本系统只提供离线决策支持与仿真，不连接或控制真实 PLC、SIS、阀门和生产设备；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区功能。

## 1. 项目概述与复杂度下限

- 项目类型：前后端分离的 B2B 全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 所有核心实体必须贯穿 PostgreSQL 表、Go model/dto/repository/service/handler/router、前端 type/api/store/page 全链路。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；后续任一实体字段、状态或权限变化都应自然触达至少 5 个文件。

## 2. 核心实体

1. `ProcessNode`（工艺节点）
   - 字段：`id`、`node_code`、`name`、`unit_name`、`medium`、`design_pressure`、`design_temperature`、`owner_team`、`status`、`created_at`、`updated_at`。
   - 关系与动作：一个节点拥有多个偏差场景和保护措施；支持建档、修改设计边界、停用及查看覆盖摘要。
   - 必须贯穿 `process_nodes` 表 -> `model/process_node.go` -> `dto/process_node.go` -> `repository/process_node_repository.go` -> `service/process_node_service.go` -> `handler/process_node_handler.go` -> `router/process_node_router.go` -> `frontend/src/types/process-node.ts` -> `api/process-node.ts` -> `stores/process-node.ts` -> 节点页面。
2. `DeviationScenario`（偏差场景）
   - 字段：`id`、`process_node_id`、`guideword`、`parameter`、`cause`、`consequence`、`likelihood`、`severity`、`scenario_state`、`version`、`created_by`、`updated_at`。
   - 关系与动作：隶属工艺节点并关联多个保护措施；支持创建版本、提交分析、退回修订和归档。
   - 必须贯穿 `deviation_scenarios` 表及对应独立 model/dto/repository/service/handler/router/type/api/store/page 文件。
3. `Safeguard`（保护措施）
   - 字段：`id`、`name`、`safeguard_type`、`target_scenario_id`、`independence_key`、`effectiveness`、`test_interval_days`、`last_verified_at`、`lifecycle_state`、`evidence_note`。
   - 关系与动作：保护一个或多个场景，同一 `independence_key` 不得在同一路径重复计数；支持验证、失效、恢复和证据追踪。
   - 必须贯穿 `safeguards` 表及对应独立 model/dto/repository/service/handler/router/type/api/store/page 文件。
4. `CoverageEvaluation`（覆盖评估）
   - 字段：`id`、`scenario_id`、`algorithm_version`、`input_snapshot`、`coverage_score`、`uncovered_paths`、`risk_rank_before`、`risk_rank_after`、`evaluation_state`、`explanation`、`evaluated_by`、`evaluated_at`。
   - 关系与动作：冻结一个场景及其保护层输入，保存可重放结果；支持运行、对比、确认和作废，禁止覆盖历史版本。
   - 必须贯穿 `coverage_evaluations` 表及对应独立 model/dto/repository/service/handler/router/type/api/store/page 文件。

## 3. 差异化算法与状态流

- 将 `ProcessNode -> DeviationScenario -> cause/consequence -> Safeguard` 构造成可解释有向图，检测没有保护层的原因到后果路径。
- 按 `independence_key` 去重保护层，结合有效性与验证有效期计算覆盖分；输出未覆盖路径、被去重措施及每一步评分依据。相同冻结输入必须得到相同结果。
- `DeviationScenario` 状态流为 `draft -> analyzed -> verified -> accepted`，`analyzed/verified -> rework`，`rework -> analyzed`；非法迁移返回 HTTP 409 并保持数据库不变。
- `CoverageEvaluation` 状态流为 `queued -> running -> completed/failed -> confirmed/voided`。运行时必须使用事务锁或幂等键避免重复结果。
- 算法只做离线辅助判断，页面和 README 必须醒目标注“结果不能替代持证工艺安全人员判断，也不能下发设备控制指令”。

## 4. 核心页面与实体消费

1. `/nodes` 工艺节点总览：消费 `ProcessNode + DeviationScenario`，展示节点边界、场景数量和风险摘要。
2. `/deviations` 偏差分析台：消费 `DeviationScenario + ProcessNode + Safeguard`，编辑原因、后果并按状态流提交。
3. `/safeguards` 保护层台账：消费 `Safeguard + DeviationScenario`，校验独立性键、有效期和覆盖目标。
4. `/coverage` 覆盖推演：消费 `CoverageEvaluation + DeviationScenario + Safeguard`，展示路径图、评分解释和版本对比。
5. `/audit` 审计中心：消费四个核心实体的变更投影及审计记录，支持实体类型、操作者、request ID 和时间筛选。

`RiskBadge` 必须被节点、偏差和覆盖页面共用；`ScenarioStateTimeline` 必须被偏差和覆盖页面共用；`EvidenceDrawer` 必须被保护层、覆盖和审计页面共用。页面不得复制这些组件的业务判断。

## 5. 横切关注点

- JWT + RBAC：角色至少为 `admin`、`process_engineer`、`safety_reviewer`、`auditor`。角色数据、JWT claims、Go `auth/rbac` 中间件、路由权限、前端路由守卫、按钮显隐和 TypeScript 类型必须联动；审计员只读，评审人与场景作者不得是同一人。
- 审计与请求追踪：所有写操作记录操作者、request ID、实体、动作、前后快照和时间；算法运行记录输入哈希、版本、耗时和结果摘要。
- 全局错误处理：统一业务错误码与 JSON 响应，区分 400、401、403、404、409、422、429、500，前端统一错误提示且不泄露堆栈。
- 接口限流与幂等：登录和算法运行接口分别限流；评估运行使用 `Idempotency-Key`，重复提交返回已有运行。

## 6. 共享枚举、组件、hooks 与中间件

- `DeviationGuideword = no | more | less | reverse | other`：后端定义在 `backend/internal/constants/deviation_guideword.go`，前端定义在 `frontend/src/types/enums/deviation-guideword.ts`。
- `CoverageState = queued | running | completed | failed | confirmed | voided`：后端定义在 `backend/internal/constants/coverage_state.go`，前端定义在 `frontend/src/types/enums/coverage-state.ts`。
- README 必须列出两组枚举在 model、dto 校验、service 状态机、前端 type/store/component/page 和测试中的全部出现位置，前后端值必须完全一致。
- 共享组件：`RiskBadge`、`ScenarioStateTimeline`、`EvidenceDrawer`，统一放在 `frontend/src/components/common/`。
- hooks：`useAuth` 管理会话和权限；`useCoverageRun` 管理幂等运行、轮询和取消过期请求，放在 `frontend/src/hooks/`。
- 后端至少实现 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go` 六个独立中间件，不得合并。

## 7. 技术、规模与文件结构

- 英文短名：`hazop-safeguard-coverage`。
- 前端：Vue 3 + TypeScript + Vite + Element Plus + Pinia；后端：Go 1.22 + Gin + GORM；正式数据库：PostgreSQL 16；runtime smoke 支持 SQLite；认证：JWT。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码目标 2500-4200 行、24-42 个功能 `.go` 文件，且必须少于 5000 行；不计 `_test.go`、生成代码和 vendor。至少提供 service、状态机和算法的单元测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根目录必须提供 `go.work` 并纳入 `./backend`，保证从项目根执行 `go work sync`、`go build ./backend/...`、`go test ./backend/...` 成功。

```text
.
├── go.work
├── runtime_smoke.json
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ util/
├── frontend/
│   ├── package.json
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四个实体在 model、dto、repository、service、handler、router、前端 api、store 和 type 中必须按实体分文件。禁止使用一个通用 CRUD 文件代替实体模块，严禁合并职责到单一文件。

## 8. 运行 Manifest、部署与验证交付

- 端口固定为前端 `18531`、后端 `19531`、PostgreSQL 宿主机 `57531`。容器内后端监听 `8080`，数据库监听 `5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20531",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20531/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端必须支持上述 SQLite smoke 配置并自动创建表和演示数据；正式 Compose 仍使用 PostgreSQL。manifest 禁止包含 `passed`、截图或执行证据字段。
- 根 `docker-compose.yml` 顶层必须是 `name: hazop-safeguard-coverage`，不得有 `version:`；包含 `db/backend/frontend`，所有 `container_name` 使用英文短名前缀，使用命名卷，数据库、后端和前端均有 healthcheck，后端等待数据库健康、前端等待后端健康。
- `.env` 与 `.env.example` 均包含 `COMPOSE_PROJECT_NAME=hazop-safeguard-coverage`、`FRONTEND_PORT=18531`、`BACKEND_PORT=19531`、`DB_PORT=57531`、`DB_DRIVER=postgres`、数据库变量和 `JWT_SECRET`；`.env` 不提交。前端只请求 `/api`，Nginx 必须原样转发 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz`，并支持 SPA 路由。
- README 首屏先给 `docker compose up -d`，并完整说明项目介绍、主要功能、演示账号及角色、架构与目录树、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、算法边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 必须实际执行：`go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`。前后端均须有 healthcheck，所有长驻服务必须 healthy。
- 启动后至少完成 8 个真实接口冒烟，覆盖 `/healthz`、登录、四实体查询、至少两个创建/更新、一次合法状态迁移、一次非法迁移 409、一次无权限 403、一次算法运行及结果读取；不能用静态假数据文件冒充接口结果。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 只允许使用 Codex 内置 Browser 验证页面与交互，禁止外部 Chrome（包括启动或调用）。至少验证登录、节点、偏差、保护层、覆盖推演五条页面流程，实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡无阻断异常。
- 所有构建、测试、runtime smoke、Compose、至少 8 条 API 的 method/path/expected/actual/assertion、Browser 页面、console/network 与关闭证据只能写入 `output/execution.md`；截图必须直接放 `output/`，至少桌面和移动端各一张并由 execution.md 引用。禁止在其他 JSON/Markdown 复制证据或伪造结果。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认本项目容器、网络和命名卷均已清理，并停止 runtime smoke 进程。
- 本项目必须独立 `git init`，并设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后执行 `git add -A && git commit -m "feat: implement and verify hazop-safeguard-coverage"`，取得实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 必须为空，不得共用 Git 仓库。
